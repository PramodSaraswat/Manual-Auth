<div class="col col-sm-6 my-3 mx-auto border mt-3">
	<div class="m-sm-4">
		<div class="text-center mt-4">
            <h1 class="h2">Sign In</h1>
            <p class="lead">
                Please Sign in to continue
            </p>
			<% if notice %>
				<div class="alert alert-warning alert-dismissible fade show" role="alert">
					<%= notice %>
					<button type="button" class="close" data-dismiss="alert" aria-label="Close">
						<span aria-hidden="true">&times;</span>
					</button>
				</div>
			<% end %>
        </div>
		<%= form_with model: @user, local: true do |form| %>
			<% if @new_user %>
				<% @new_user.errors.full_messages.each do |message| %>
				<div class="alert alert-warning alert-dismissible fade show" role="alert">
					<%= message %>
					<button type="button" class="close" data-dismiss="alert" aria-label="Close">
						<span aria-hidden="true">&times;</span>
					</button>
				</div>
				<% end %>
			<% end %>
		
			<div class="form-group">
				<label>Email</label>
				<%= form.text_field :email , :class => "form-control"%>
			</div>
		
			<div class="form-group">
				<label>Password</label>
				<%= form.password_field :password , :class => "form-control"%>
			</div>
			
			<div class="mt-2 mb-3">
				<%= form.submit "Sign In", :class => "btn btn-primary px-4"%>
			</div>
            <small style="font-size:15px;">
                Don't have account?
                <a href="<%= account_signup_path %>">Create One!</a>
            </small><br>
            <small style="font-size:15px;">
                <a href="<%= password_forget_password_path %>">Forgot Password?</a>
            </small>
            
		<% end %>
		
	</div>
</div>
